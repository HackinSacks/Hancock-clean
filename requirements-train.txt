# QLoRA fine-tune deps for RTX 5070 (~12GB). Install AFTER requirements.txt.
# Blackwell (sm_120) may need a CUDA nightly torch wheel — see README.
torch>=2.4.0
transformers>=4.50.0
datasets>=2.20.0
peft>=0.14.0
trl>=0.24.0
accelerate>=1.0.0
bitsandbytes>=0.48.0
sentencepiece>=0.2.0
huggingface_hub>=0.24.0
